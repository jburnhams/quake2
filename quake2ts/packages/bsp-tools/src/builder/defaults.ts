import { CONTENTS_SOLID } from '@quake2ts/shared';
import type { TextureDef } from './types.js';

export const DEFAULT_TEXTURE: TextureDef = {
  name: 'base1/basewall',
  offsetX: 0,
  offsetY: 0,
  rotation: 0,
  scaleX: 1,
  scaleY: 1,
};

export const DEFAULT_CONTENTS = CONTENTS_SOLID;
