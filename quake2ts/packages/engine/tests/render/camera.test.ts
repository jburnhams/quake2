
import { describe, it, expect } from 'vitest';
import * as camera from '../../src/render/camera';

describe('camera', () => {
    it('should have a test', () => {
        expect(true).toBe(true);
    });
});
