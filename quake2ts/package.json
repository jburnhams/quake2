{
  "name": "quake2ts",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@8.15.7",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.5.3"
  }
}
